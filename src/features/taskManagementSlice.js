import { createSlice } from "@reduxjs/toolkit";
import data from '../data/data.json'

export const taskManagementSlice = createSlice({
    name: "taskManagementSlice",
    initialState: {},
    reducers: {
        
    }
})